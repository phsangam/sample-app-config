apiVersion: v1
kind: Service
metadata:
  name: java-service
  namespace: uat
spec:
  type: {{ .Values.service.type }}
  selector:
    app: java-service
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
